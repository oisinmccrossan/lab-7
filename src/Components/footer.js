// Define the Footer component
const Footer = () => {
  // Return a heading element with the text "My Footer in another component"
  return <h1>My Footer in another component</h1>;
};

// Export the Footer component as the default export
export default Footer;